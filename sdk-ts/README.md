# v-contractx TypeScript SDK

This SDK mirrors the kernel request shapes for client-side development.

## Capabilities

- build projection requests,
- submit projection and reconstruction calls,
- preview a contract's acceptance logic,
- render a projection panel in React.

## Notes

The SDK does not perform authoritative validation. It prepares requests for the kernel and presents previews for the UI.
